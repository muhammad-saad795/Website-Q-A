[phases.setup]
nixPkgs = ["python311", "bash"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "PLAYWRIGHT_BROWSERS_PATH=/app/pw-browsers playwright install --with-deps chromium"
]

[start]
cmd = "PLAYWRIGHT_BROWSERS_PATH=/app/pw-browsers bash start.sh"
