[phases.setup]
nixPkgs = ["...", "python311", "playwright-driver"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install --with-deps chromium"]

[start]
cmd = "gunicorn app:app --worker-class gthread --threads 4 --timeout 120"
