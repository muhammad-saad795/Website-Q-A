.venv/
.env
__pycache__/
*.pyc
job_results/
crawl_results/
server.log
result.json
# SQLite DB is runtime state — not committed to git.
# On Railway, mount a persistent volume at /data and set API__DB_PATH=/data/jobs.db
*.db
